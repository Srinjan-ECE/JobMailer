{
  "name": "jobmailer-netlify",
  "version": "2.0.0",
  "description": "AI-powered fresher job digest — Netlify hosted",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build needed'"
  },
  "dependencies": {
    "nodemailer": "^6.9.7",
    "node-fetch": "^2.7.0"
  }
}
